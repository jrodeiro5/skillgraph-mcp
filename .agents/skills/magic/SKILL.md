---
name: magic
description: "21st.dev Magic: AI-powered UI component generation and design system tools."
allowed-tools: Bash(skillgraph-mcp probe *)
---

# magic

21st.dev Magic: AI-powered UI component generation and design system tools.

## Health

!`skillgraph-mcp probe --config /home/javi/.config/skillgraph-mcp/mcp.json magic`

## Routing

This skill's tools live behind the **skillgraph-mcp** gateway. To use them:

1. Call `use_skill("magic")` via the `skillgraph-mcp` MCP server to inspect tool signatures.
2. Invoke tools inside `execute_code(...)` — they are exposed as Python functions.

Do **not** call `readMcpResource(server: "magic")` directly; the gateway does the routing.

_Generated by `skillgraph-mcp generate-skills`. Do not edit by hand — changes will be overwritten._
