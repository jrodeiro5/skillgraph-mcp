---
name: wsl-terminal
description: WSL2 terminal access. Run shell commands inside WSL2 without firewall or NAT issues.
allowed-tools: Bash(skillgraph-mcp probe *)
---

# wsl-terminal

WSL2 terminal access. Run shell commands inside WSL2 without firewall or NAT issues.

## Health

!`skillgraph-mcp probe --config /home/javi/.config/skillgraph-mcp/mcp.json wsl-terminal`

## Routing

This skill's tools live behind the **skillgraph-mcp** gateway. To use them:

1. Call `use_skill("wsl-terminal")` via the `skillgraph-mcp` MCP server to inspect tool signatures.
2. Invoke tools inside `execute_code(...)` — they are exposed as Python functions.

Do **not** call `readMcpResource(server: "wsl-terminal")` directly; the gateway does the routing.

_Generated by `skillgraph-mcp generate-skills`. Do not edit by hand — changes will be overwritten._
