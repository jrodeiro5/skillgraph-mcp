---
name: brave-search
description: Web search via Brave. Discover URLs, articles, and resources. Use before firecrawl to extract content.
allowed-tools: Bash(skillgraph-mcp probe *)
---

# brave-search

Web search via Brave. Discover URLs, articles, and resources. Use before firecrawl to extract content.

## Health

!`skillgraph-mcp probe --config /home/javi/.config/skillgraph-mcp/mcp.json brave-search`

## Routing

This skill's tools live behind the **skillgraph-mcp** gateway. To use them:

1. Call `use_skill("brave-search")` via the `skillgraph-mcp` MCP server to inspect tool signatures.
2. Invoke tools inside `execute_code(...)` — they are exposed as Python functions.

Do **not** call `readMcpResource(server: "brave-search")` directly; the gateway does the routing.

_Generated by `skillgraph-mcp generate-skills`. Do not edit by hand — changes will be overwritten._
