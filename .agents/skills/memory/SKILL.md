---
name: memory
description: Cross-session memory for agents. Store, retrieve, and delete persistent facts across conversations.
allowed-tools: Bash(skillgraph-mcp probe *)
---

# memory

Cross-session memory for agents. Store, retrieve, and delete persistent facts across conversations.

## Health

!`skillgraph-mcp probe --config /home/javi/.config/skillgraph-mcp/mcp.json memory`

## Routing

This skill's tools live behind the **skillgraph-mcp** gateway. To use them:

1. Call `use_skill("memory")` via the `skillgraph-mcp` MCP server to inspect tool signatures.
2. Invoke tools inside `execute_code(...)` — they are exposed as Python functions.

Do **not** call `readMcpResource(server: "memory")` directly; the gateway does the routing.

_Generated by `skillgraph-mcp generate-skills`. Do not edit by hand — changes will be overwritten._
