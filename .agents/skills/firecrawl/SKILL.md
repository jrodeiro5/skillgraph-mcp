---
name: firecrawl
description: Web scraping and content extraction. Scrape, crawl, and extract structured data from any URL.
allowed-tools: Bash(skillgraph-mcp probe *)
---

# firecrawl

Web scraping and content extraction. Scrape, crawl, and extract structured data from any URL.

## Health

!`skillgraph-mcp probe --config /home/javi/.config/skillgraph-mcp/mcp.json firecrawl`

## Routing

This skill's tools live behind the **skillgraph-mcp** gateway. To use them:

1. Call `use_skill("firecrawl")` via the `skillgraph-mcp` MCP server to inspect tool signatures.
2. Invoke tools inside `execute_code(...)` — they are exposed as Python functions.

Do **not** call `readMcpResource(server: "firecrawl")` directly; the gateway does the routing.

_Generated by `skillgraph-mcp generate-skills`. Do not edit by hand — changes will be overwritten._
