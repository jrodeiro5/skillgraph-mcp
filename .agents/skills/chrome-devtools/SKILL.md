---
name: chrome-devtools
description: Chrome DevTools protocol. Inspect DOM, capture console errors, network requests, and profile performance.
allowed-tools: Bash(skillgraph-mcp probe *)
---

# chrome-devtools

Chrome DevTools protocol. Inspect DOM, capture console errors, network requests, and profile performance.

## Health

!`skillgraph-mcp probe --config /home/javi/.config/skillgraph-mcp/mcp.json chrome-devtools`

## Routing

This skill's tools live behind the **skillgraph-mcp** gateway. To use them:

1. Call `use_skill("chrome-devtools")` via the `skillgraph-mcp` MCP server to inspect tool signatures.
2. Invoke tools inside `execute_code(...)` — they are exposed as Python functions.

Do **not** call `readMcpResource(server: "chrome-devtools")` directly; the gateway does the routing.

_Generated by `skillgraph-mcp generate-skills`. Do not edit by hand — changes will be overwritten._
