---
name: sequential-thinking
description: Structured sequential reasoning. Break complex problems into ordered thought chains.
allowed-tools: Bash(skillgraph-mcp probe *)
---

# sequential-thinking

Structured sequential reasoning. Break complex problems into ordered thought chains.

## Health

!`skillgraph-mcp probe --config /home/javi/.config/skillgraph-mcp/mcp.json sequential-thinking`

## Routing

This skill's tools live behind the **skillgraph-mcp** gateway. To use them:

1. Call `use_skill("sequential-thinking")` via the `skillgraph-mcp` MCP server to inspect tool signatures.
2. Invoke tools inside `execute_code(...)` — they are exposed as Python functions.

Do **not** call `readMcpResource(server: "sequential-thinking")` directly; the gateway does the routing.

_Generated by `skillgraph-mcp generate-skills`. Do not edit by hand — changes will be overwritten._
